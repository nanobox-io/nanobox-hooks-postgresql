HOME=<%= APP_DIR %>
PATH=/opt/gonano/sbin:/opt/gonano/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
<%
  crons.each do |job|
-%>
<%= sanitize_cron(job[:schedule]) %> root /opt/nanobox/cron/<%= job[:id] %>
<% 
  end
-%>